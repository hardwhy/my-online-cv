{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "supabase",
  "projectType": "library",
  "sourceRoot": "packages/supabase/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/packages/supabase"],
      "options": {
        "command": "tsc -p packages/supabase/tsconfig.json",
        "cwd": "{workspaceRoot}"
      }
    }
  },
  "tags": ["type:lib", "scope:data"]
}
